{
  "name": "site-drop",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently -n web,api -c cyan,magenta \"npm run dev --workspace @sitedrop/web\" \"npm run dev --workspace @sitedrop/api\"",
    "dev:web": "npm run dev --workspace @sitedrop/web",
    "dev:api": "npm run dev --workspace @sitedrop/api",
    "build": "npm run build --workspace @sitedrop/api && npm run build --workspace @sitedrop/web",
    "start": "npm run start --workspace @sitedrop/api"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
